<VirtualHost *:80> 
    DocumentRoot "C:/laragon/www/dist"
    ServerName dist.test
    ServerAlias *.dist.test
    <Directory "C:/laragon/www/dist">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

# If you want to use SSL, enable it by going to Menu > Apache > SSL > Enabled
